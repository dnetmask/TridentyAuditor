__pycache__/
*.pyc
.venv/
.env
.pytest_cache/
.coverage
htmlcov/
*.egg-info/

# Build del frontend copiado aquí por el Dockerfile multi-stage (ver
# backend/Dockerfile) — nunca se genera fuera de una imagen Docker o de una
# verificación manual, no debe versionarse.
app/static/frontend/
