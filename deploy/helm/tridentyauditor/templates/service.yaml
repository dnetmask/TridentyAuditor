apiVersion: v1
kind: Service
metadata:
  name: {{ include "tridentyauditor.fullname" . }}
  labels:
    {{- include "tridentyauditor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "tridentyauditor.selectorLabels" . | nindent 4 }}
